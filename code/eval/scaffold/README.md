# Evaluation scaffolds

Files here are **not** published measurements. They hold methodology drafts or
modeled series until replaced by a live harness run.

Do **not** copy into `../published/` without a real measurement and a
`measured_at` timestamp. Locked 8/10/12 MiB full-stack cells remain under
`../published/fullstack-latest.json`.
